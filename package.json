{
  "name": "satp-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Public keyless search API over the SATP registry (Cloudflare Workers + KV)",
  "scripts": {
    "build": "node build.mjs",
    "test": "node tests.mjs",
    "deploy": "node deploy.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}